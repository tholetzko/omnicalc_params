<h1>
  Loan Payment
</h1>

<dd>
  A <%=@years %> year loan of $<%= number_with_delimiter(@principal, :delimiter => ',') %> with an annual interest rate of <%= @apr %>% requires a monthly payment of $<%= number_with_delimiter(@monthly_payment_output, :delimiter => ',')%>
</dd>
